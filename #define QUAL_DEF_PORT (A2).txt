#define QUAL_DEF_PORT (A2)
#define QUAL_DEF_PRINT_PREC   (3)
#define QUAL_PREC (1000)
#define ADC_MAX (1023)
#define QUAL_MAX  (30)

class MyQualSensor {
public:
   MyQualSensor() {
      changePort(QUAL_DEF_PORT);
      changePrintPrec(QUAL_DEF_PRINT_PREC);
   }

   MyQualSensor(int nPort) {
      changePort(nPort);
      changePrintPrec(QUAL_DEF_PRINT_PREC);
   }

   MyQualSensor(int nPort, int nPrec) {
      changePort(nPort);
      changePrintPrec(nPrec);
   }

   ~MyQualSensor() {

   }
   void lloop()
   {
      double RRR = getAnalogQual();
      if (qqual > 29)
         digitalWrite(4, HIGH);
      else
         digitalWrite(4, LOW);
   }
};